DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
OPENAI_API_KEY=""
UPLOAD_DIR="./data/uploads"

# Production / Vercel: use a Postgres URL (postgresql://...) and the same AUTH_SECRET
# on every deploy. Local OCR (tesseract) is optional; typed capture always works.
